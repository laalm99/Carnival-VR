using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveTarget : MonoBehaviour
{
    [SerializeField] private GameObject target;
    void Update()
    {
        //Mathf.Clamp(target.transform.position.x, );
    }
}
